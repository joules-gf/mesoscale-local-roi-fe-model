# Future work

Repo-specific follow-ups captured during workflow cleanup.

1. Investigate the error that occurs when `<restart> True </restart>` is set in the XML input file.
2. Copy the final/resolved input XML into each simulation output folder for reproducibility.

These notes were moved out of `.hermes/` so the repository no longer tracks Hermes-specific planning metadata.
